school-dashboard/
├── package.json
├── vite.config.js                 # Vite + proxy config (/api → :5000, /uploads → :5000)
├── tailwind.config.js
├── postcss.config.js
├── .env                           # PORT=5000 (optional)
│
├── server/                        # Node.js + Express backend
│   ├── index.js                   # Entry point — registers all routes + CORS
│   ├── db.js                      # SQLite setup, all table definitions, seed data
│   └── routes/
│       ├── auth.js                # POST /login, /logout, /change-password, GET /me
│       ├── students.js            # /api/students  — CRUD + board filter
│       ├── teachers.js            # /api/teachers  — CRUD
│       ├── expenses.js            # /api/expenses  — CRUD + month/year/category filter
│       ├── categories.js          # /api/categories — expense category CRUD
│       ├── progress.js            # /api/progress  — exam marks CRUD + summary
│       └── attendance.js          # /api/attendance — daily marking + student summary
│
├── server/uploads/                # Profile photos (auto-created, served statically)
│
├── src/                           # React frontend (Vite + Tailwind)
│   ├── main.jsx                   # React entry point
│   ├── App.jsx                    # Router + ProtectedRoute wrapper
│   ├── index.css                  # Tailwind base styles
│   │
│   ├── components/                # Shared UI components
│   │   ├── Sidebar.jsx            # Nav links, user menu, logout, change password
│   │   ├── Modal.jsx              # Reusable modal (sm / md / lg / xl sizes)
│   │   ├── ProtectedRoute.jsx     # Redirects to /login if not authenticated
│   │   ├── StatCard.jsx
│   │   ├── Table.jsx
│   │   └── SearchBar.jsx
│   │
│   ├── modules/                   # Feature modules
│   │   ├── auth/
│   │   │   └── LoginPage.jsx      # Login form — navigates to / on success
│   │   ├── home/
│   │   │   └── HomePage.jsx       # Dashboard — stats, greeting, announcement, backup
│   │   ├── students/
│   │   │   ├── StudentsPage.jsx   # Directory — board+class cascading filter, table
│   │   │   ├── StudentForm.jsx    # Add/Edit — board dropdown, all fields
│   │   │   ├── StudentPrintView.jsx # Profile modal — 4 tabs + 🖨️ Print Report
│   │   │   └── ProgressReport.jsx # Exam marks entry, accordion, grades, summary cards
│   │   ├── teachers/
│   │   │   └── TeachersPage.jsx   # Directory — CRUD, filters, 🖨️ Print List
│   │   ├── expenses/
│   │   │   └── ExpensesPage.jsx   # Expenses — CRUD, category manager, 🖨️ Print Report
│   │   ├── attendance/
│   │   │   └── AttendancePage.jsx # Daily attendance — date picker, bulk mark, P/A/L toggle
│   │   └── contact/
│   │       └── ContactPage.jsx    # School details + contact form
│   │
│   ├── services/                  # API call layer (all fetch calls live here)
│   │   ├── authService.js         # login, logout, getMe, changePassword
│   │   ├── studentService.js      # getStudents, createStudent, fees CRUD
│   │   ├── teacherService.js      # getTeachers, createTeacher, etc.
│   │   ├── expenseService.js      # getExpenses, createExpense, etc.
│   │   ├── categoryService.js     # getCategories, createCategory, deleteCategory
│   │   ├── progressService.js     # getProgress, addMark, updateMark, deleteExam
│   │   └── attendanceService.js   # getAttendanceForDate, saveBulkAttendance, getSummary
│   │
│   ├── store/                     # Zustand global state (one store per domain)
│   │   ├── authStore.js           # token, user, isAuthenticated (localStorage persisted)
│   │   ├── studentStore.js        # students, fees, filters (board + class)
│   │   ├── teacherStore.js        # teachers, filters
│   │   ├── expenseStore.js        # expenses, filters, grandTotal
│   │   ├── progressStore.js       # marks, summary, groupedByExam
│   │   └── attendanceStore.js     # records, draft (unsaved toggles), date, filters
│   │
│   └── utils/
│       └── printUtils.js          # printStudent(), printTeachers(), printExpenses()
│                                  # Opens styled print window — no external deps
│
└── school.db                      # SQLite file (auto-created on first run)
